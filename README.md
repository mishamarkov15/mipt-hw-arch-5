# Изменения 

Страница скачивается один раз вместо ~7865 раз, слова из страницы считаются одним проходом, список слов дедуплицируется с сохранением порядка вывода.

## Тесты

- Реальный запуск: около `1.03s`.
- Бенчмарк алгоритма: `16.2x` быстрее, `22.1x` меньше пик памяти.